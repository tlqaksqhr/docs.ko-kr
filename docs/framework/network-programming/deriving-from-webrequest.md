---
title: "WebRequest에서 파생"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
caps.latest.revision: "9"
author: mcleblanc
ms.author: markl
manager: markl
ms.workload: dotnet
ms.openlocfilehash: 83579c25c154462cb21488acf9fcf84999b9a2d4
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 12/22/2017
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="3111a-102">WebRequest에서 파생</span><span class="sxs-lookup"><span data-stu-id="3111a-102">Deriving from WebRequest</span></span>
<span data-ttu-id="3111a-103"><xref:System.Net.WebRequest> 클래스는 .NET Framework 플러그형 프로토콜 모델에 적합한 프로토콜별 요청 처리기를 만들기 위한 기본 메서드 및 속성을 제공하는 추상 기본 클래스입니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="3111a-104">**WebRequest** 클래스를 사용하는 응용 프로그램은 사용되는 프로토콜을 지정할 필요 없이 지원되는 모든 프로토콜을 사용하여 데이터를 요청할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="3111a-105">프로토콜별 클래스를 플러그형 프로토콜로 사용하려면 다음 두 가지 조건을 충족해야 합니다. 클래스는 <xref:System.Net.IWebRequestCreate> 인터페이스를 구현하고 <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> 메서드에 등록되어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="3111a-106">클래스는 플러그형 인터페이스를 제공하도록 **WebRequest**의 모든 추상 메서드 및 속성을 재정의해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="3111a-107">**WebRequest** 인스턴스는 일회용입니다. 또 다른 요청을 만들려면 새 **WebRequest**를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="3111a-108">**WebRequest**는 개발자가 템플릿 **WebRequest**를 직렬화하고 나서 추가 요청을 위한 템플릿을 다시 구성할 수 있도록 <xref:System.Runtime.Serialization.ISerializable> 인터페이스를 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="3111a-109">IWebRequest Create 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="3111a-110"><xref:System.Net.IWebRequestCreate.Create%2A> 메서드는 프로토콜별 클래스의 새 인스턴스를 초기화해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="3111a-111">새 **WebRequest**가 생성되면 <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> 메서드는 요청된 URI를 **RegisterPrefix** 메서드에 등록된 URI 접두사와 일치시킵니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="3111a-112">적절한 프로토콜별 하위 항목의 **Create** 메서드는 프로토콜별 필드를 수정할 필요 없이 프로토콜에 대한 표준 요청/응답 트랜잭션을 수행할 수 있는 하위 항목의 초기화된 인스턴스를 반환해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="3111a-113">ConnectionGroupName 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="3111a-114"><xref:System.Net.WebRequest.ConnectionGroupName%2A> 속성을 사용하여 단일 연결을 통해 여러 요청이 생성될 수 있도록 리소스 연결 그룹의 이름을 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="3111a-115">연결 공유를 구현하려면 연결 풀링 및 할당에 대한 프로토콜별 메서드를 사용해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="3111a-116">예를 들어 제공된 <xref:System.Net.ServicePointManager> 클래스는 <xref:System.Net.HttpWebRequest> 클래스에 대한 연결 공유를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="3111a-117">**ServicePointManager** 클래스는 각 연결 그룹의 특정 서버에 대한 연결을 제공하는 <xref:System.Net.ServicePoint>를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="3111a-118">ContentLength 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-118">ContentLength Property</span></span>  
 <span data-ttu-id="3111a-119"><xref:System.Net.WebRequest.ContentLength%2A> 속성은 데이터를 업로드할 때 서버에 전송될 데이터 크기(바이트)를 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="3111a-120">일반적으로 **ContentLength** 속성이 0보다 큰 값으로 설정될 경우 업로드가 수행되도록 지정하려면 <xref:System.Net.WebRequest.Method%2A> 속성을 설정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="3111a-121">ContentType 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-121">ContentType Property</span></span>  
 <span data-ttu-id="3111a-122"><xref:System.Net.WebRequest.ContentType%2A> 속성은 보내는 콘텐츠 형식을 식별하기 위해 프로토콜을 통해 서버에 보내야 하는 특수 정보를 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="3111a-123">일반적으로 이것은 업로드된 데이터의 MIME 콘텐츠 형식입니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="3111a-124">Credentials 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-124">Credentials Property</span></span>  
 <span data-ttu-id="3111a-125"><xref:System.Net.WebRequest.Credentials%2A> 속성에는 서버에서 요청을 인증하는 데 필요한 정보가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="3111a-126">프로토콜에 대한 인증 프로세스의 세부 정보를 구현해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="3111a-127"><xref:System.Net.AuthenticationManager> 클래스는 요청을 인증하고 인증 토큰을 제공해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="3111a-128">프로토콜에서 사용되는 자격 증명을 제공하는 클래스는 <xref:System.Net.ICredentials> 인터페이스를 구현해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="3111a-129">Headers 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-129">Headers Property</span></span>  
 <span data-ttu-id="3111a-130"><xref:System.Net.WebRequest.Headers%2A> 속성에는 요청과 연결된 메타데이터 이름/값 쌍의 임의 컬렉션이 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="3111a-131">이름/값 쌍으로 표시될 수 있는 프로토콜에 필요한 모든 메타데이터가 **Headers** 속성에 포함될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="3111a-132">일반적으로 이 정보는 <xref:System.Net.WebRequest.GetRequestStream%2A> 또는 <xref:System.Net.WebRequest.GetResponse%2A> 메서드를 호출하기 전에 설정해야 합니다. 요청이 생성된 후에는 메타데이터를 읽기 전용으로 간주합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="3111a-133">헤더 메타데이터를 사용하기 위해 **Headers** 속성을 사용할 필요가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="3111a-134">프로토콜별 메타데이터는 속성으로 노출됩니다. 예를 들어 <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> 속성은 **User-Agent** HTTP 헤더를 노출합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="3111a-135">헤더 메타데이터를 속성으로 노출할 경우 **Headers** 속성을 사용하여 동일한 속성을 설정하도록 허용하면 안 됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="3111a-136">Method 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-136">Method Property</span></span>  
 <span data-ttu-id="3111a-137"><xref:System.Net.WebRequest.Method%2A> 속성에는 요청을 통해 서버에서 수행하도록 요청할 동사 또는 작업이 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="3111a-138">**Method** 속성의 기본값은 프로토콜별 속성을 설정할 필요 없이 표준 요청/응답 작업을 사용하도록 설정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="3111a-139">예를 들어 <xref:System.Net.HttpWebResponse.Method%2A> 메서드는 기본적으로 웹 서버에서 리소스를 요청하고 응답을 반환하는 GET으로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="3111a-140">일반적으로 **Method** 속성이 업로드가 수행 중임을 나타내는 동사 또는 작업으로 설정될 경우 **ContentLength** 속성을 0보다 큰 값으로 설정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="3111a-141">PreAuthenticate 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="3111a-142">응용 프로그램은 <xref:System.Net.WebRequest.PreAuthenticate%2A> 속성을 설정하여 인증 질문을 기다리지 않고 초기 요청을 통해 인증 정보를 보내도록 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="3111a-143">**PreAuthenticate** 속성은 프로토콜이 초기 요청을 통해 전송된 인증 자격 증명을 지원하는 경우에만 유효합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="3111a-144">Proxy 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-144">Proxy Property</span></span>  
 <span data-ttu-id="3111a-145"><xref:System.Net.WebRequest.Proxy%2A> 속성에는 요청된 리소스에 액세스하는 데 사용되는 <xref:System.Net.IWebProxy> 인터페이스가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="3111a-146">**Proxy** 속성은 프로토콜이 프록시 설정된 요청을 지원할 경우에만 유효합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="3111a-147">프로토콜에 필요한 경우 기본 프록시를 설정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="3111a-148">회사 방화벽을 사용하는 것과 같은 일부 환경에서는 프로토콜에 프록시를 사용해야 할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="3111a-149">이 경우 **IWebProxy** 인터페이스를 구현하여 프로토콜에 작동하는 프록시 클래스를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="3111a-150">RequestUri 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-150">RequestUri Property</span></span>  
 <span data-ttu-id="3111a-151"><xref:System.Net.WebRequest.RequestUri%2A> 속성에는 **WebRequest.Create** 메서드에 전달된 URI가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="3111a-152">이 속성은 읽기 전용이고 **WebRequest**가 생성된 후에는 변경할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="3111a-153">프로토콜이 리디렉션을 지원할 경우 다른 URI로 식별되는 리소스에서 응답이 나올 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="3111a-154">응답한 URI에 대한 액세스 권한을 제공해야 할 경우 해당 URI가 포함된 추가 속성을 제공해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="3111a-155">제한 시간 속성</span><span class="sxs-lookup"><span data-stu-id="3111a-155">Timeout Property</span></span>  
 <span data-ttu-id="3111a-156"><xref:System.Net.WebRequest.Timeout%2A> 속성에는 요청 시간이 초과되고 예외가 throw되기 전에 대기할 시간 길이(밀리초)가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="3111a-157">**Timeout**은 <xref:System.Net.WebRequest.GetResponse%2A> 메서드를 통해 생성된 비동기 요청에만 적용되고, 비동기 요청은 <xref:System.Net.WebRequest.Abort%2A> 메서드를 사용하여 보류 중인 요청을 취소해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="3111a-158">**Timeout** 속성 설정은 프로토콜별 클래스가 시간 제한 프로세스를 구현할 경우에만 유효합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="3111a-159">Abort 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-159">Abort Method</span></span>  
 <span data-ttu-id="3111a-160"><xref:System.Net.WebRequest.Abort%2A> 메서드는 서버에 대한 보류 중인 비동기 요청을 취소합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="3111a-161">요청이 취소된 후 **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream** 또는 **EndGetRequestStream**을 호출하면 <xref:System.Net.WebException>이 throw되고 <xref:System.Net.WebException.Status%2A> 속성이 <xref:System.Net.WebExceptionStatus>로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="3111a-162">BeginGetRequestStream 및 EndGetRequestStream 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="3111a-163"><xref:System.Net.WebRequest.BeginGetRequestStream%2A> 메서드는 데이터를 서버에 업로드하는 데 사용되는 스트림에 대한 비동기 요청을 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="3111a-164"><xref:System.Net.WebRequest.EndGetRequestStream%2A> 메서드는 비동기 요청을 완료하고 요청된 스트림을 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="3111a-165">이러한 메서드는 표준 .NET Framework 비동기 패턴을 사용하여 **GetRequestStream** 메서드를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="3111a-166">BeginGetResponse 및 EndGetResponse 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="3111a-167"><xref:System.Net.WebRequest.BeginGetResponse%2A> 메서드는 서버에 대한 비동기 요청을 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="3111a-168"><xref:System.Net.WebRequest.EndGetResponse%2A> 메서드는 비동기 요청을 완료하고 요청된 응답을 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="3111a-169">이러한 메서드는 표준 .NET Framework 비동기 패턴을 사용하여 **GetResponse** 메서드를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="3111a-170">GetRequestStream 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="3111a-171"><xref:System.Net.WebRequest.GetRequestStream%2A> 메서드는 요청된 서버에 데이터를 쓰는 데 사용되는 스트림을 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="3111a-172">반환된 스트림은 검색되지 않는 쓰기 전용 스트림이어야 하고 서버에 기록되는 단방향 데이터 스트림으로 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="3111a-173">스트림은 <xref:System.IO.Stream.CanRead%2A> 및 <xref:System.IO.Stream.CanSeek%2A> 속성에 대해 false를 반환하고 <xref:System.IO.Stream.CanWrite%2A> 속성에 대해 true를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="3111a-174">**GetRequestStream** 메서드는 대개 서버에 대한 연결을 열고 스트림을 반환하기 전에 데이터가 서버에 전송되고 있음을 나타내는 헤더 정보를 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="3111a-175">**GetRequestStream**은 요청을 시작하므로 **GetRequestStream**을 호출한 후에는 일반적으로 **Header** 속성이나 **ContentLength** 속성을 설정할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="3111a-176">GetResponse 메서드</span><span class="sxs-lookup"><span data-stu-id="3111a-176">GetResponse Method</span></span>  
 <span data-ttu-id="3111a-177"><xref:System.Net.WebRequest.GetResponse%2A> 메서드는 서버의 응답을 나타내는 <xref:System.Net.WebResponse> 클래스의 프로토콜별 하위 항목을 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="3111a-178">요청이 이미 **GetRequestStream** 메서드를 통해 시작된 경우가 아니면 **GetResponse** 메서드는 **RequestUri**로 식별되는 리소스 연결을 만들고, 생성되는 요청의 형식을 나타내는 헤더 정보를 보내고 나서, 리소스의 응답을 받습니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="3111a-179">**GetResponse** 메서드가 호출된 후에는 모든 속성을 읽기 전용으로 간주해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="3111a-180">**WebRequest** 인스턴스는 일회용이고, 또 다른 요청을 만들려면 새 **WebRequest**를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="3111a-181">**GetResponse** 메서드는 들어오는 응답을 포함할 적절한 **WebResponse** 하위 항목을 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="3111a-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="3111a-182">참고 항목</span><span class="sxs-lookup"><span data-stu-id="3111a-182">See Also</span></span>  
 <xref:System.Net.WebRequest>  
 <xref:System.Net.HttpWebRequest>  
 <xref:System.Net.FileWebRequest>  
 [<span data-ttu-id="3111a-183">플러그형 프로토콜 프로그래밍</span><span class="sxs-lookup"><span data-stu-id="3111a-183">Programming Pluggable Protocols</span></span>](../../../docs/framework/network-programming/programming-pluggable-protocols.md)  
 [<span data-ttu-id="3111a-184">WebResponse에서 파생</span><span class="sxs-lookup"><span data-stu-id="3111a-184">Deriving from WebResponse</span></span>](../../../docs/framework/network-programming/deriving-from-webresponse.md)
