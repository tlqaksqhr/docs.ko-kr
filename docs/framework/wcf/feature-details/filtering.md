---
title: 필터링
ms.date: 03/30/2017
ms.assetid: 4002946c-e34a-4356-8cfb-e25912a4be63
ms.openlocfilehash: 5f599ac74aa63951f59c5e5c79d3fe37b2ab5100
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 05/04/2018
ms.locfileid: "33497245"
---
# <a name="filtering"></a><span data-ttu-id="dd199-102">필터링</span><span class="sxs-lookup"><span data-stu-id="dd199-102">Filtering</span></span>
<span data-ttu-id="dd199-103">Windows Communication Foundation (WCF) 시스템 필터링 선언적 필터를 사용 메시지를 일치 하 고 작업을 결정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-103">The Windows Communication Foundation (WCF) filtering system can use declarative filters to match messages and make operational decisions.</span></span> <span data-ttu-id="dd199-104">필터를 사용하여 메시지 일부를 검사하고 메시지를 통해 수행할 작업을 결정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-104">You can use filters to determine what to do with a message by examining part of the message.</span></span> <span data-ttu-id="dd199-105">예를 들어, 큐 프로세스에서는 XPath 1.0 쿼리를 사용하여 알려진 헤더의 우선 순위 요소를 검사함으로써 메시지를 큐의 앞으로 이동할지 여부를 결정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-105">A queuing process, for example, can use an XPath 1.0 query to check the priority element of a known header to determine whether to move a message to the front of the queue.</span></span>  
  
 <span data-ttu-id="dd199-106">필터링 시스템은 효율적으로 수행할 수 있는 클래스의 집합으로 구성를 필터 집합을 결정 `true` 특정 WCF 메시지에 대 한 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-106">The filtering system is composed of a set of classes that can efficiently determine which of a set of filters are `true` for a particular WCF message.</span></span>  
  
 <span data-ttu-id="dd199-107">필터링 시스템은 WCF 메시징;의 핵심 구성 요소 매우 빠른 되도록 설계 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-107">The filtering system is a core component of WCF messaging; it is designed to be extremely fast.</span></span> <span data-ttu-id="dd199-108">각 필터 구현은 WCF 메시지에 대 한 종류의 일치 하는 특정 작업에 대해 최적화 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-108">Each filter implementation has been optimized for a particular kind of matching against WCF messages.</span></span>  
  
 <span data-ttu-id="dd199-109">필터링 시스템은 스레드로부터 안전하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-109">The filtering system is not thread safe.</span></span> <span data-ttu-id="dd199-110">응용 프로그램은 모든 잠금 의미 체계를 처리해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-110">The application must handle any locking semantics.</span></span> <span data-ttu-id="dd199-111">그러나 다중 판독기, 단일 작성기를 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-111">It does, however, support a multi-reader, single writer.</span></span>  
  
## <a name="where-filtering-fits"></a><span data-ttu-id="dd199-112">필터링이 적용되는 위치</span><span class="sxs-lookup"><span data-stu-id="dd199-112">Where Filtering Fits</span></span>  
 <span data-ttu-id="dd199-113">메시지를 수신하고 메시지를 적절한 응용 프로그램 구성 요소에 발송하는 프로세스의 일부인 경우 필터링이 수행됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-113">Filtering is performed after a message is received and is part of the process of dispatching the message to the proper application component.</span></span> <span data-ttu-id="dd199-114">필터링 시스템의 디자인에 몇 가지 WCF 하위 시스템 포함 메시징, 라우팅, 보안, 이벤트 처리 및 시스템 관리의 요구 사항을 해결 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-114">The design of the filtering system addresses the requirements of several WCF subsystems, including messaging, routing, security, event handling, and system management.</span></span>  
  
## <a name="filters"></a><span data-ttu-id="dd199-115">필터</span><span class="sxs-lookup"><span data-stu-id="dd199-115">Filters</span></span>  
 <span data-ttu-id="dd199-116">필터 엔진에는 두 가지 기본 구성 요소 즉, 필터와 필터 테이블이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-116">The filter engine has two primary components, filters and filter tables.</span></span> <span data-ttu-id="dd199-117">필터는 사용자 지정 논리 조건에 기반한 메시지에 대해 부울을 결정합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-117">A filter makes Boolean decisions about a message based on user-specified logical conditions.</span></span> <span data-ttu-id="dd199-118">필터는 <xref:System.ServiceModel.Dispatcher.MessageFilter> 클래스를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-118">Filters implement the <xref:System.ServiceModel.Dispatcher.MessageFilter> class.</span></span>  
  
 <span data-ttu-id="dd199-119"><xref:System.ServiceModel.Dispatcher.MessageFilter.Match%2A> 메서드는 메시지가 필터를 충족하는지 확인하는 데 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-119">The <xref:System.ServiceModel.Dispatcher.MessageFilter.Match%2A> methods are used to determine if a message satisfies a filter.</span></span> <span data-ttu-id="dd199-120">메서드 중 하나가 메시지의 헤더를 테스트하지만 메시지 본문은 검사할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-120">One of the methods tests the message's header, but cannot inspect the message body.</span></span> <span data-ttu-id="dd199-121">다른 메서드는 *메시지 버퍼* 입력된 매개 변수로 가져오고 메시지 본문을 검사할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-121">The other method takes a *message buffer* as an input parameter and can inspect the message body.</span></span>  
  
 <span data-ttu-id="dd199-122">필터는 보통 개별적으로 테스트되지 않고, <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601.CreateFilterTable%2A> 메서드가 만드는 제네릭 클래스인 필터 테이블의 일부로 테스트됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-122">Filters are not usually tested individually, but as part of a filter table, which is a generic class that the <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601.CreateFilterTable%2A> method creates.</span></span>  
  
 <span data-ttu-id="dd199-123">여러 종류의 필터마다 특정 유형의 부울 조건에 대해 일치하도록 특수화됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-123">The several kinds of filters each specialize in matching on a particular kind of Boolean condition.</span></span> <span data-ttu-id="dd199-124">필터를 일단 생성하고 나면 필터가 사용하는 기준을 변경할 수 없습니다. 필터 기준을 수정하려면 새 필터를 생성하고 기존 필터를 삭제합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-124">Once you construct a filter, you cannot change the criteria that a filter uses; to modify a filter's criteria, construct a new one and delete the existing filter.</span></span>  
  
### <a name="action-filters"></a><span data-ttu-id="dd199-125">작업 필터</span><span class="sxs-lookup"><span data-stu-id="dd199-125">Action Filters</span></span>  
 <span data-ttu-id="dd199-126"><xref:System.ServiceModel.Dispatcher.ActionMessageFilter>에는 작업 문자열 목록이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-126">The <xref:System.ServiceModel.Dispatcher.ActionMessageFilter> contains a list of action strings.</span></span> <span data-ttu-id="dd199-127">필터 목록의 작업이 메시지 또는 메시지 버퍼의 작업 헤더와 일치하는 경우 `Match` 메서드는 `true`를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-127">If any of the actions in the filter’s list matches the Action header in the message or message buffer, the `Match` method returns `true`.</span></span> <span data-ttu-id="dd199-128">목록이 비어 있는 경우 필터는 모두 일치하는 필터로 간주되고 모든 메시지 또는 메시지 버퍼가 일치하며 `Match`는 `true`를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-128">If the list is empty, the filter is considered a match-all filter and any message or message buffer matches and `Match` returns `true`.</span></span> <span data-ttu-id="dd199-129">필터 목록의 작업이 메시지 또는 메시지 버퍼의 작업 헤더와 일치하지 않는 경우 `Match`는 `false`를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-129">If none of the actions in the filter’s list matches the Action header in the message or message buffer, `Match` returns `false`.</span></span> <span data-ttu-id="dd199-130">메시지에 작업이 없고 필터 목록이 비어 있지 않은 경우 `Match`는 `false`를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-130">If there is no action in the message and the filter’s list is non-empty, then `Match` returns `false`.</span></span>  
  
### <a name="endpoint-address-filters"></a><span data-ttu-id="dd199-131">끝점 주소 필터</span><span class="sxs-lookup"><span data-stu-id="dd199-131">Endpoint Address Filters</span></span>  
 <span data-ttu-id="dd199-132"><xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>는 헤더 컬렉션에 표시된 대로 끝점 주소를 기준으로 메시지 및 메시지 버퍼를 필터링합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-132">The <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> filters messages and message buffers based on an endpoint address, as represented in their header collection.</span></span> <span data-ttu-id="dd199-133">메시지가 이러한 필터를 통과하려면 다음 조건을 충족해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-133">For a message to pass such a filter, the following conditions must be met:</span></span>  
  
-   <span data-ttu-id="dd199-134">필터 주소 URI(Uniform Resource Identifier)가 메시지 받는 사람 헤더의 URI와 동일해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-134">The filter’s address Uniform Resource Identifier (URI) must be the same as the one in the message To header.</span></span>  
  
-   <span data-ttu-id="dd199-135">필터 주소의 각 끝점 매개 변수(`address.Headers` 컬렉션)가 매핑할 메시지에서 헤더를 찾아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-135">Each endpoint parameter in the address of the filter (`address.Headers` collection) must find a header in the message to map on.</span></span> <span data-ttu-id="dd199-136">메시지 또는 메시지 버퍼의 추가 헤더는 `true`를 유지하기 위해 일치에 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-136">Extra headers in the message or message buffer are acceptable for the match to remain `true`.</span></span>  
  
### <a name="prefix-endpoint-address-filters"></a><span data-ttu-id="dd199-137">접두사 끝점 주소 필터</span><span class="sxs-lookup"><span data-stu-id="dd199-137">Prefix Endpoint Address Filters</span></span>  
  
1.  <span data-ttu-id="dd199-138"><xref:System.ServiceModel.Dispatcher.PrefixEndpointAddressMessageFilter>는 메시지 URI의 접두사에 위치할 수 있는 일치를 제외하고 <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> 필터와 동일하게 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-138">The <xref:System.ServiceModel.Dispatcher.PrefixEndpointAddressMessageFilter> functions just like the <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter> filter, except that the match can be on a prefix of the message URI.</span></span> <span data-ttu-id="dd199-139">예를 들어 주소를 지정 하는 필터 http://www.adatum.com 주소가 지정 된 메시지를 일치 http://www.adatum.com/userA합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-139">For example, a filter specifying the address http://www.adatum.com matches messages addressed to http://www.adatum.com/userA.</span></span>  
  
### <a name="xpath-message-filters"></a><span data-ttu-id="dd199-140">XPath 메시지 필터</span><span class="sxs-lookup"><span data-stu-id="dd199-140">XPath Message Filters</span></span>  
 <span data-ttu-id="dd199-141"><xref:System.ServiceModel.Dispatcher.XPathMessageFilter>는 XPath 식을 사용하여 XML 문서에 특정 요소, 특성, 텍스트 또는 기타 XML 구문을 포함할지 여부를 결정합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-141">An <xref:System.ServiceModel.Dispatcher.XPathMessageFilter> uses an XPath expression to determine whether an XML document contains specific elements, attributes, text, or other XML syntactic constructs.</span></span> <span data-ttu-id="dd199-142">필터는 엄격한 XPath의 하위 집합에 대해 매우 효율적으로 최적화됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-142">The filter is optimized to be extremely efficient for a strict subset of XPath.</span></span> <span data-ttu-id="dd199-143">에 설명 된 XML 경로 언어는 [W3C XML 경로 언어 1.0 사양](http://go.microsoft.com/fwlink/?LinkId=94779)합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-143">The XML Path Language is described in the [W3C XML Path Language 1.0 specification](http://go.microsoft.com/fwlink/?LinkId=94779).</span></span>  
  
 <span data-ttu-id="dd199-144">일반적으로 응용 프로그램은 끝점에서 <xref:System.ServiceModel.Dispatcher.XPathMessageFilter>를 사용하여 SOAP 메시지의 내용을 퀴리한 다음 해당 쿼리의 결과를 기준으로 적절한 작업을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-144">Typically, an application uses an <xref:System.ServiceModel.Dispatcher.XPathMessageFilter> at an endpoint to query the contents of a SOAP message and then takes the appropriate action based on the results of that query.</span></span> <span data-ttu-id="dd199-145">예를 들어, 큐 프로세스에서는 XPath 쿼리를 사용하여 알려진 헤더의 우선 순위 요소를 검사함으로써 메시지를 큐 앞으로 이동할지 여부를 결정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-145">A queuing process, for example, may use an XPath query to inspect the priority element of a known header to decide whether to move a message to the front of the queue.</span></span>  
  
## <a name="filter-tables"></a><span data-ttu-id="dd199-146">필터 테이블</span><span class="sxs-lookup"><span data-stu-id="dd199-146">Filter Tables</span></span>  
 <span data-ttu-id="dd199-147">필터 테이블은 키-값 쌍을 저장하는 데 사용되며 여기서 필터는 키이고 일부 관련 데이터가 값입니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-147">Filter tables are used to store key-value pairs, where a filter is the key and some associated data is the value.</span></span> <span data-ttu-id="dd199-148">필터 데이터는 메시지가 필터와 일치하고 필터 데이터의 유형이 필터 테이블 클래스의 일반 매개 변수인 경우 수행할 작업을 표시하는 데 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-148">The filter data can be used to indicate what actions to take if a message matches the filter and the type of the filter data is the generic parameter for the filter table class.</span></span> <span data-ttu-id="dd199-149">필터 데이터는 라우팅 규칙, 세션 보안 상태, 채널 상의 수신기 등으로 구성할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-149">The filter data can consist of routing rules, session security state, listeners on a channel, and so on.</span></span> <span data-ttu-id="dd199-150">데이터는 데이터 흐름 제어가 필요한 경우 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-150">The data can be used where data flow control is necessary.</span></span>  
  
 <span data-ttu-id="dd199-151">필터 테이블은 제네릭 인터페이스 <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>을 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-151">Filter tables implement the generic interface <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>.</span></span>  
  
 <span data-ttu-id="dd199-152">필터 테이블에는 테이블의 모든 필터에 대한 메시지와 일치하고 일치하는 필터 또는 데이터의 순서 지정이 없는 컬렉션을 반환하는 여러 메서드가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-152">Filter tables have several methods that match a message against all the filters in the table and return an unordered collection of matching filters or data.</span></span> <span data-ttu-id="dd199-153">일부 일치 메서드는 다중 일치이며 일치하는 모든 항목을 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-153">Some of the match methods are multiple-match and return all matching items.</span></span> <span data-ttu-id="dd199-154">다른 메서드는 하나의 항목만 반환하는 단일 일치이며 필터가 두 개 이상 일치할 경우 <xref:System.ServiceModel.Dispatcher.MultipleFilterMatchesException>을 throw합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-154">Others are single-match, returning only one item, and throw a <xref:System.ServiceModel.Dispatcher.MultipleFilterMatchesException> if more than one filter matches.</span></span>  
  
### <a name="message-filter-table"></a><span data-ttu-id="dd199-155">메시지 필터 테이블</span><span class="sxs-lookup"><span data-stu-id="dd199-155">Message Filter Table</span></span>  
 <span data-ttu-id="dd199-156"><xref:System.ServiceModel.Dispatcher.MessageFilterTable%601>은 <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>의 가장 일반적인 구현입니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-156">The <xref:System.ServiceModel.Dispatcher.MessageFilterTable%601> is the most general implementation of <xref:System.ServiceModel.Dispatcher.IMessageFilterTable%601>.</span></span> <span data-ttu-id="dd199-157">테이블에 모든 형식의 필터를 저장할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-157">You can store filters of all types in the table.</span></span>  
  
 <span data-ttu-id="dd199-158">숫자로 된 우선 순위를 필터에 할당할 수 있으며 가장 큰 숫자가 제일 높은 우선 순위입니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-158">You can assign numeric priorities to filters, where the highest priority is signified by the highest number.</span></span> <span data-ttu-id="dd199-159">여러 형식의 필터가 동일한 우선 순위를 가질 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-159">Multiple types of filters can have the same priority.</span></span> <span data-ttu-id="dd199-160">특정 형식의 필터가 둘 이상의 우선 순위 수준으로 표시될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-160">A particular type of filter can appear in more than one priority level.</span></span>  
  
 <span data-ttu-id="dd199-161">일치는 제일 높은 우선 순위부터 시작하여 수행되며 제공된 우선 순위와 일치하는 필터를 찾으면 하위 우선 순위를 가진 필터는 검사하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-161">Matching is done starting with the highest priority, and once matching filters are found with a given priority, no filters with lower priorities are examined.</span></span> <span data-ttu-id="dd199-162">따라서 단일 필터 일치 메서드를 사용하고 둘 이상의 필터가 메시지와 일치하지만 일치하는 각 필터의 우선 순위가 서로 다른 경우, 예외가 throw되지 않고 가장 높은 순위를 가진 필터가 반환됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-162">Therefore, if you are using a single-filter match method, and more than one filter matches a message, but each matching filter has a different priority, then no exception is thrown and the filter with the highest priority is returned.</span></span> <span data-ttu-id="dd199-163">마찬가지로 다중 필터 일치 메서드는 가장 높은 순위를 가진 필터와 일치하는 필터만 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-163">Similarly, a multiple-filter match method returns only those matching filters with the highest priority.</span></span>  
  
### <a name="xpath-message-filter-table"></a><span data-ttu-id="dd199-164">XPath 메시지 필터 테이블</span><span class="sxs-lookup"><span data-stu-id="dd199-164">XPath Message Filter Table</span></span>  
 <span data-ttu-id="dd199-165"><xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601>은 선언적 XPath 필터에 대해서 최적화되기 때문에 테이블 키가 <xref:System.ServiceModel.Dispatcher.XPathMessageFilter>입니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-165">The <xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> is optimized for declarative XPath filters, so the table key is a <xref:System.ServiceModel.Dispatcher.XPathMessageFilter>.</span></span>  
  
 <span data-ttu-id="dd199-166"><xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> 클래스는 메시징 시나리오 대부분을 포함하는 XPath의 하위 집합에 대한 일치를 최적화하고, 전체 XPath 1.0 문법도 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-166">The <xref:System.ServiceModel.Dispatcher.XPathMessageFilterTable%601> class optimizes matching for a subset of XPath that covers most of the messaging scenarios and also supports the full XPath 1.0 grammar.</span></span> <span data-ttu-id="dd199-167">효율적인 병렬 일치를 위한 알고리즘을 최적화합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-167">It has optimized algorithms for efficient parallel matching.</span></span>  
  
 <span data-ttu-id="dd199-168">이 테이블에는 `Match` 및 <xref:System.Xml.XPath.XPathNavigator>를 통해 수행되는 여러 특수화된 <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> 메서드가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-168">This table has several specialized `Match` methods that operate over an <xref:System.Xml.XPath.XPathNavigator> and a <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator>.</span></span> <span data-ttu-id="dd199-169"><xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator>는 <xref:System.Xml.XPath.XPathNavigator> 속성을 추가하여 <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator.CurrentPosition%2A> 클래스를 확장합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-169">A <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> extends the <xref:System.Xml.XPath.XPathNavigator> class by adding a <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator.CurrentPosition%2A> property.</span></span> <span data-ttu-id="dd199-170">이 속성을 사용하면 XML 문서 내의 위치를 저장해 놓은 후 탐색기를 복제하지 않고 신속하게 로드할 수 있습니다. 이 작업을 <xref:System.Xml.XPath.XPathNavigator>에서 수행하려면 비용이 많이 드는 메모리 할당이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-170">This property allows positions within the XML document to be saved and loaded quickly without having to clone the navigator, an expensive memory allocation that the <xref:System.Xml.XPath.XPathNavigator> requires for such an operation.</span></span> <span data-ttu-id="dd199-171">WCF XPath 엔진은 XML 문서에 대해 쿼리를 실행 하는 과정에서 커서의 위치를 자주 기록해 야 하므로 <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> 메시지 처리를 위한 중요 한 최적화를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-171">The WCF XPath engine must frequently record the position of the cursor in the course of executing queries on XML documents, so the <xref:System.ServiceModel.Dispatcher.SeekableXPathNavigator> provides an important optimization for message processing.</span></span>  
  
## <a name="customer-scenarios"></a><span data-ttu-id="dd199-172">고객 시나리오</span><span class="sxs-lookup"><span data-stu-id="dd199-172">Customer Scenarios</span></span>  
 <span data-ttu-id="dd199-173">메시지에 포함된 데이터에 따라 메시지를 다른 프로세싱 모듈에 전송하려는 경우 언제든지 필터링을 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-173">You can use filtering any time you want to send a message to different processing modules depending on data contained in the message.</span></span> <span data-ttu-id="dd199-174">두 가지 일반적인 시나리오는 동작 코드에 기반한 메시지 라우팅과 메시지 끝점 주소에 기반한 메시지 스트림의 역 멀티플렉싱입니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-174">Two typical scenarios are routing a message based on its action code and de-multiplexing a stream of messages based on the messages' endpoint address.</span></span>  
  
### <a name="routing"></a><span data-ttu-id="dd199-175">라우팅</span><span class="sxs-lookup"><span data-stu-id="dd199-175">Routing</span></span>  
 <span data-ttu-id="dd199-176">끝점의 수신기는 메시지 SOAP 헤더에서 하나 이상의 동작 코드가 있는 메시지를 수신 대기합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-176">The listener of an endpoint listens for messages that have one or more action codes in the message's SOAP header.</span></span> <span data-ttu-id="dd199-177">동작 코드가 들어 있는 배열을 생성자에게 전달하고 <xref:System.ServiceModel.Dispatcher.ActionMessageFilter>를 만들어 이를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-177">You implement this by creating an <xref:System.ServiceModel.Dispatcher.ActionMessageFilter> by passing an array that contains the action codes to its constructor.</span></span> <span data-ttu-id="dd199-178">해당 필터를 사용하여 `ListenerFactory`에 등록하기 때문에 동작이 필터의 동작과 일치하는 메시지만 해당 특정 끝점에 도달합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-178">It uses that filter to register with the `ListenerFactory`, so only messages whose action matches one of those in the filter get to that particular endpoint.</span></span>  
  
### <a name="de-multiplexing"></a><span data-ttu-id="dd199-179">역 멀티플렉싱</span><span class="sxs-lookup"><span data-stu-id="dd199-179">De-multiplexing</span></span>  
 <span data-ttu-id="dd199-180">여러 개의 끝점이 동일한 `ServiceListener`에서 분리되어 연결이 끊어진 경우 메시지를 역 멀티플렉싱하고 특정 끝점 주소에 속하는지 여부를 확인하는 유일한 방법은 <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>를 사용하는 것입니다. 여기서 헤더에 저장된 정보를 조회하여 등록된 끝점 쪽에 있는 메시지를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-180">When multiple endpoints fan out from the same `ServiceListener` off the wire, the only way to de-multiplex messages and know whether they belong to a certain endpoint address, is to use <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>s, which select messages toward the registered endpoints by performing a lookup on the information stored in the headers.</span></span> <span data-ttu-id="dd199-181">이러한 필터의 경우 전달된 메시지에만 다음 두 가지 모두에 해당하는 필요한 모든 헤더가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="dd199-181">In these filters, only those messages that pass have all necessary headers that correspond to both:</span></span>  
  
-   <span data-ttu-id="dd199-182">`EndpointAddress`의 URI.</span><span class="sxs-lookup"><span data-stu-id="dd199-182">The URI in the `EndpointAddress`.</span></span>  
  
-   <span data-ttu-id="dd199-183">`EndpointAddress`에 지정된 대로 <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>의 나머지 끝점 매개 변수.</span><span class="sxs-lookup"><span data-stu-id="dd199-183">The rest of the endpoint parameters in the `EndpointAddress` as specified in the <xref:System.ServiceModel.Dispatcher.EndpointAddressMessageFilter>.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="dd199-184">참고 항목</span><span class="sxs-lookup"><span data-stu-id="dd199-184">See Also</span></span>  
 [<span data-ttu-id="dd199-185">데이터 전송 및 Serialization</span><span class="sxs-lookup"><span data-stu-id="dd199-185">Data Transfer and Serialization</span></span>](../../../../docs/framework/wcf/feature-details/data-transfer-and-serialization.md)
