---
title: Visual Basic에서 ienumerable 인터페이스를 구현합니다.
ms.custom: ''
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: ''
ms.suite: ''
ms.technology:
- devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
caps.latest.revision: 15
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 4645153f9c830bc96b7ec55367f46f09098eb78d
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/26/2018
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="1eec4-102">연습: Visual Basic에서 IEnumerable(Of T) 구현</span><span class="sxs-lookup"><span data-stu-id="1eec4-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="1eec4-103"><xref:System.Collections.Generic.IEnumerable%601> 인터페이스는 한 번에 하나의 항목 값의 시퀀스를 반환할 수 있는 클래스에서 구현 됩니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="1eec4-104">데이터를 한 번에 한 항목은 작업을 메모리에 완전 한 데이터 집합을 로드할 필요가 없습니다 반환 활용 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="1eec4-105">충분 한 메모리를 사용 하 여 데이터에서 단일 항목을 로드 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="1eec4-106">구현 하는 클래스는 `IEnumerable(T)` 인터페이스를 함께 사용할 수 있습니다 `For Each` 루프 또는 LINQ 쿼리 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="1eec4-107">예를 들어 큰 텍스트 파일을 읽고 특정 검색 기준과 일치 하는 파일에서 각 줄을 반환 해야 하는 응용 프로그램을 고려해 야 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="1eec4-108">응용 프로그램 LINQ 쿼리를 사용 하 여 지정된 된 조건과 일치 하는 파일의 줄을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="1eec4-109">LINQ 쿼리를 사용 하 여 파일의 내용을 퀴리, 응용 프로그램 배열 또는 컬렉션으로는 파일의 내용을 로드할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="1eec4-110">그러나 배열 또는 컬렉션에 전체 파일을 로드 필요한 것 보다 훨씬 더 많은 메모리를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="1eec4-111">대신 LINQ 쿼리 검색 조건과 일치 하는 값만 반환 하는 열거 가능한 클래스를 사용 하 여 파일 내용을 쿼리할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="1eec4-112">일부만 반환 하는 쿼리 훨씬 적은 메모리를 사용할 일치 하는 값입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="1eec4-113">구현 하는 클래스를 만들 수는 <xref:System.Collections.Generic.IEnumerable%601> 열거 가능한 데이터와 원본 데이터를 노출 하는 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="1eec4-114">클래스를 구현 하는 `IEnumerable(T)` 인터페이스를 구현 하는 다른 클래스를 필요로 합니다는 <xref:System.Collections.Generic.IEnumerator%601> 원본 데이터에서 반복 하는 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="1eec4-115">이러한 두 클래스를 사용 하 여 데이터 항목을 특정 형식으로 순차적으로 반환할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="1eec4-116">이 연습에서는 구현 하는 클래스를 만듭니다는 `IEnumerable(Of String)` 인터페이스와 구현 하는 클래스는 `IEnumerator(Of String)` 인터페이스를 한 줄씩 텍스트 파일을 한 번에 읽을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="1eec4-117">열거 가능한 클래스 만들기</span><span class="sxs-lookup"><span data-stu-id="1eec4-117">Creating the Enumerable Class</span></span>  
  
|<span data-ttu-id="1eec4-118">Enumerable 클래스 프로젝트를 만들려면</span><span class="sxs-lookup"><span data-stu-id="1eec4-118">To create the enumerable class project</span></span>|  
|---|  
|<span data-ttu-id="1eec4-119">1.  Visual basic의 경우에 **파일** 메뉴에서 **새로** 클릭 하 고 **프로젝트**합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-119">1.  In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span><br /><span data-ttu-id="1eec4-120">2.  **새 프로젝트** 대화 상자의 **프로젝트 형식** 창에서 **Windows**가 선택되었는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-120">2.  In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="1eec4-121">**템플릿** 창에서 **클래스 라이브러리**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="1eec4-122">**이름** 상자에 `StreamReaderEnumerable`를 입력한 다음 **확인**을 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="1eec4-123">새 프로젝트가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-123">The new project is displayed.</span></span><br /><span data-ttu-id="1eec4-124">3.  **솔루션 탐색기**Class1.vb 파일을 마우스 오른쪽 단추로 클릭 하 고 클릭 **이름 바꾸기**합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-124">3.  In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="1eec4-125">파일 이름을 `StreamReaderEnumerable.vb`으로 바꾸고 ENTER 키를 누릅니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="1eec4-126">파일 이름을 바꾸면 클래스 이름도 `StreamReaderEnumerable`으로 바뀝니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="1eec4-127">이 클래스는 `IEnumerable(Of String)` 인터페이스를 구현합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-127">This class will implement the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="1eec4-128">4.  StreamReaderEnumerable 프로젝트를 마우스 오른쪽 단추로 클릭, 가리킨 **추가**, 클릭 하 고 **새 항목**합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-128">4.  Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="1eec4-129">선택 된 **클래스** 템플릿.</span><span class="sxs-lookup"><span data-stu-id="1eec4-129">Select the **Class** template.</span></span> <span data-ttu-id="1eec4-130">에 **이름** 상자에 입력 합니다 `StreamReaderEnumerator.vb` 클릭 **확인**합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>|  
  
 <span data-ttu-id="1eec4-131">이 프로젝트의 첫 번째 클래스의 열거 가능한 클래스 이며 구현는 `IEnumerable(Of String)` 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="1eec4-132">이 제네릭 인터페이스를 구현 하는 <xref:System.Collections.IEnumerable> 인터페이스와이 클래스의 소비자로 입력 된 값에 액세스할 수 있도록 보증 `String`합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
|<span data-ttu-id="1eec4-133">IEnumerable을 구현 하는 코드를 추가 하려면</span><span class="sxs-lookup"><span data-stu-id="1eec4-133">To add the code to implement IEnumerable</span></span>|  
|---|  
|<span data-ttu-id="1eec4-134">1.  StreamReaderEnumerable.vb 파일을 엽니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-134">1.  Open the StreamReaderEnumerable.vb file.</span></span><br /><span data-ttu-id="1eec4-135">2.  다음 줄에 `Public Class StreamReaderEnumerable`를 입력 한 다음 ENTER 키를 누릅니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-135">2.  On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]<br />     <span data-ttu-id="1eec4-136">Visual Basic에서 필요로 하는 멤버를 사용 하 여 클래스를 자동으로 채우려고는 `IEnumerable(Of String)` 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="1eec4-137">3.  열거 가능한이 클래스는 한 번에는 한 줄씩 텍스트 파일에서에서 줄을 읽습니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-137">3.  This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="1eec4-138">다음 코드는 파일 경로 입력된 매개 변수로 사용 하는 공용 생성자를 노출 하는 클래스를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]<br /><span data-ttu-id="1eec4-139">4.  구현에서 <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> 의 메서드는 `IEnumerable(Of String)` 인터페이스의 새 인스턴스를 반환 합니다는 `StreamReaderEnumerator` 클래스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-139">4.  Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="1eec4-140">구현에서 `GetEnumerator` 의 메서드는 `IEnumerable` 인터페이스를 만들 수 있습니다 `Private`의 멤버를 노출 해야 하기 때문에는 `IEnumerable(Of String)` 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="1eec4-141">Visual Basic에 대해 생성 된 코드는 `GetEnumerator` 메서드에 다음 코드를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]|  
  
|<span data-ttu-id="1eec4-142">IEnumerator를 구현 하는 코드를 추가 하려면</span><span class="sxs-lookup"><span data-stu-id="1eec4-142">To add the code to implement IEnumerator</span></span>|  
|---|  
|<span data-ttu-id="1eec4-143">1.  StreamReaderEnumerator.vb 파일을 엽니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-143">1.  Open the StreamReaderEnumerator.vb file.</span></span><br /><span data-ttu-id="1eec4-144">2.  다음 줄에 `Public Class StreamReaderEnumerator`를 입력 한 다음 ENTER 키를 누릅니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-144">2.  On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]<br />     <span data-ttu-id="1eec4-145">Visual Basic에서 필요로 하는 멤버를 사용 하 여 클래스를 자동으로 채우려고는 `IEnumerator(Of String)` 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span><br /><span data-ttu-id="1eec4-146">3.  열거자 클래스 텍스트 파일이 열리고 파일의 파일에서 줄을 읽을 수는 I/O를 수행 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-146">3.  The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="1eec4-147">클래스는 파일 경로 입력된 매개 변수로 사용 하는 공용 생성자를 노출 하 고 읽기에 대 한 텍스트 파일을 열에 다음 코드를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]<br /><span data-ttu-id="1eec4-148">4.  `Current` 둘 다에 대 한 속성의 `IEnumerator(Of String)` 및 `IEnumerator` 으로 텍스트 파일에서 현재 항목을 반환 하는 인터페이스는 `String`합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-148">4.  The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="1eec4-149">구현에서 `Current` 속성의는 `IEnumerator` 인터페이스를 만들 수 있습니다 `Private`의 멤버를 노출 해야 하기 때문에는 `IEnumerator(Of String)` 인터페이스입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="1eec4-150">Visual Basic에 대해 생성 된 코드는 `Current` 다음 코드를 사용 하 여 속성입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]<br /><span data-ttu-id="1eec4-151">5.  `MoveNext` 의 메서드는 `IEnumerator` 인터페이스 텍스트 파일에서 다음 항목으로 이동 하 고 반환 되는 값을 업데이트 하는 `Current` 속성입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-151">5.  The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="1eec4-152">읽기, 더 많은 항목이 없는 경우는 `MoveNext` 메서드 반환 `False`고, 그렇지 않으면는 `MoveNext` 메서드가 반환 되 `True`합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="1eec4-153">`MoveNext` 메서드에 다음 코드를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-153">Add the following code to the `MoveNext` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]<br /><span data-ttu-id="1eec4-154">6.  `Reset` 의 메서드는 `IEnumerator` 인터페이스는 반복기를 텍스트 파일의 시작 부분을 가리키도록 지시 하 고 현재 항목 값을 지웁니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-154">6.  The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="1eec4-155">`Reset` 메서드에 다음 코드를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-155">Add the following code to the `Reset` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]<br /><span data-ttu-id="1eec4-156">7.  `Dispose` 의 메서드는 `IEnumerator` 인터페이스 보장 하는 반복기를 제거 하기 전에 관리 되지 않는 리소스를 모두 해제 됩니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-156">7.  The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="1eec4-157">사용 되는 파일 핸들은 `StreamReader` 개체는 관리 되지 않는 리소스 반복기 인스턴스를 제거 하기 전에 닫혀 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="1eec4-158">Visual Basic에 대해 생성 된 코드는 `Dispose` 메서드를 다음 코드로 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)]|  
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="1eec4-159">샘플 반복기 사용</span><span class="sxs-lookup"><span data-stu-id="1eec4-159">Using the Sample Iterator</span></span>  
 <span data-ttu-id="1eec4-160">열거 가능한 클래스를 사용 하 여 구현 하는 개체를 필요로 하는 제어 구조와 함께 코드에서 `IEnumerable`와 같은 `For Next` 루프 또는 LINQ 쿼리입니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="1eec4-161">다음 예제와 `StreamReaderEnumerable` LINQ 쿼리에서 합니다.</span><span class="sxs-lookup"><span data-stu-id="1eec4-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="1eec4-162">참고 항목</span><span class="sxs-lookup"><span data-stu-id="1eec4-162">See Also</span></span>  
 [<span data-ttu-id="1eec4-163">Visual Basic의 LINQ 소개</span><span class="sxs-lookup"><span data-stu-id="1eec4-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="1eec4-164">제어 흐름</span><span class="sxs-lookup"><span data-stu-id="1eec4-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="1eec4-165">루프 구조</span><span class="sxs-lookup"><span data-stu-id="1eec4-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="1eec4-166">For Each...Next 문</span><span class="sxs-lookup"><span data-stu-id="1eec4-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
